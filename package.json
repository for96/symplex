{
  "name": "simplesso",
  "version": "0.1.0",
  "private": true,
  "description": "Simulatore didattico del metodo del simplesso, della dualità e dei tagli (Gomory/cover) per corsi di Ricerca Operativa.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "watch": "node build.mjs --watch",
    "test:cnf": "node scripts/test-cnf.mjs",
    "icons": "node scripts/generate-icons.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "sharp": "^0.33.5"
  }
}
